# 验证记录 · 2026-09-05

以下按阶段记录验证结果；较早阶段的“未部署公网”描述由文末的 Linux 部署记录更新。

环境：Windows、Node.js 24.11.1，单进程，最终端口 3030。

- `npm run check`：28 项测试通过，包含卡池、规则、贵族与终局、隐藏信息、支付、房间权限、重复请求、在线/离线、多标签重连、AI异常、限流以及真实 HTTP SSE。
- 10 个独立 HTTP 客户端分布于 3 个房间，加入与会话隔离通过；SSE 连接/断开正确改变在线状态。
- 独立只读审查进行了 200 局本地 AI 模拟；未观察到非法动作或卡死。另单独验证 2/3/4 人本地策略对局均能结算。这不能保证 AI 强度或所有随机局面均有最佳策略。
- 真实浏览器验证：更改昵称、创建房间、邀请 DeepSeek/本地 AI、开始、拿取筹码、预留、获取黄金、用黄金购买预留牌、增加永久折扣、刷新恢复、退出房间。
- 实际 DeepSeek API：使用服务端现有环境密钥完成一次真实决策；浏览器状态确认 `source=deepseek`，未走 fallback。没有打印、记录或复制密钥。
- 浏览器控制台：功能验收阶段未发现 error/warn。
- 桌面与 390×844 手机视口检查；手机内容宽度等于视口内容宽度，无横向溢出。临时视口覆盖已恢复。
- 服务重启与最终健康检查通过。仅启动本地服务，未配置公网域名、TLS或防火墙；Dockerfile与反向代理配置提供作部署参考，未执行 Docker 构建。

已处理审查发现的终局边界、贵族后15分、返还重复贵族、盲抽日志泄漏、AI返还组合合法性、匿名会话速率限制与AI调度外层异常。

## 侧栏与回合补充更新

- 新增右侧个人宝石库，涵盖持有宝石、永久折扣、预留与已购牌；资源足够的市场/预留牌高亮。
- 悬停和键盘聚焦不足额卡牌显示短缺颜色和黄金抵扣后总缺口；足额牌不显示 tooltip。
- “所需支付”仅减永久折扣，实际黄金替代方案独立展示。
- 空筹码池且不可购买时可手动跳过；连续一圈跳过结算；房主可确认强制结束并取消 AI 请求。
- `npm run check`：38 项通过，包含额外价格、缺口、跳过、房主权限及 AI 迟到结果测试。
- 在独立的 127.0.0.1:3031 测试房间完成浏览器验证：跳过推进至下一轮、房主结束进入结算、保留席位，白2蓝1价格不出现黄金、足额牌无提示、不足额牌提示准确。390px 手机视口没有横向溢出，控制台无 error/warn。
- 经用户确认，已重启 3030 主服务并清空旧房间；新服务健康检查返回 HTTP 200。独立预览服务已停止，临时脚本和日志已清理。
- 本轮独立只读审查未发现阻塞需求的回归。

## 横屏紧凑布局更新

- 左栏保留玩家头像、昵称及在线状态；房主移出入口保留。对局记录改为弹窗，左栏显示最新一条记录。
- 拿取与返还筹码放在我的宝石库顶部。持有筹码、永久折扣、等效宝石采用相同五色行；等效值为持有加折扣，黄金独立列出，不重复计入五色等效值。
- 预留的三张牌常驻侧栏；已购牌数量常驻，全集通过弹窗查看，避免卡牌积累撑高主页面。
- 独立四人局面包含三张预留、十五张已购牌和 AI 完成状态，在 1280×720、1366×768、1920×1080 下，文档宽高均等于视口，侧栏无溢出。卡牌 SVG 正常加载。
- 浏览器验证已购牌弹窗展示十五张牌、记录弹窗、三色拿取、超过十枚后返还一枚并推进回合；等效值同步更新。1280×720 预留牌费用与顶部信息无重叠。
- 390×844 手机视口无横向溢出；浏览器控制台无 error/warn。38 项测试通过。
- 3030 服务已恢复运行，健康检查 HTTP 200；本次未修改后端规则。独立验收服务结束后停止。

## Linux 部署记录 · 2026-09-06

- 已部署到 Ubuntu 的 `/opt/splendor`，使用独立 Node.js 24.11.1 和 `splendor` 系统用户，systemd 服务已启用开机自启。
- 应用监听 `127.0.0.1:3030`，Nginx 沿用现有 HTTPS 证书，入口为 `https://taskstream.xyz/splendor/`。前端资源、API、SSE 和邀请链接已适配子路径。
- 部署时验证了 Nginx 配置测试、`/splendor` 的 308 跳转、首页及 CSS 的 HTTP 200、健康接口 `{"ok":true}`；公网页面创建房间成功，并显示“已连接”。
- 用户补充服务器密钥后已重启服务；检查确认配置项存在、服务 active、健康接口正常。这些检查未包含一次服务器端 DeepSeek 实际决策，不能据此确认密钥有效或模型调用成功。
- GitHub 仓库为 `https://github.com/Tucyan/splendor-private-table`；当前服务器仍为压缩包安装，尚未切换到 Git 拉取更新。
