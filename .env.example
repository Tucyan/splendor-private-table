# Node.js 22+，复制为 .env 后按需填写。不要提交 .env。
PORT=3030
HOST=0.0.0.0
# 优先读取本机环境变量 deepseekkey，也支持下面的名称。
# DEEPSEEK_API_KEY=your-key-here
DEEPSEEK_MODEL=deepseek-v4-flash
# HTTPS 反向代理部署时启用；本地 HTTP 请保持 false。
COOKIE_SECURE=false
